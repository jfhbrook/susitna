{
  "name": "matanuska",
  "version": "0.0.1",
  "description": "a shell-friendly dialect of BASIC",
  "main": "index.js",
  "bin": {
    "matbas": "bin/matbas"
  },
  "scripts": {
    "build": "concurrently 'npm run build:versions' 'npm run build:ast' && npm run build:typescript",
    "build:versions": "bash ./scripts/versions.sh",
    "build:typescript": "tsc -p tsconfig.build.json",
    "build:citree": "cd tools/citree && npm run build",
    "build:ast": "citree ast/index.citree",
    "check": "concurrently 'npm run build:versions' 'npm run build:ast' && tsc --noEmit",
    "format": "prettier --write \"**/*.ts\"",
    "start": "npm run build && PATH=\"${PATH}:$(pwd)/bin\" bin/matbas",
    "lint": "prettier --list-different \"**.ts\" && eslint --fix \"**.ts\"",
    "lint:shell": "shellcheck ./bin/* ./scripts/* ./.husky/pre-commit",
    "lint:staged": "lint-staged",
    "test": "npm run build:versions && NO_TRACE=1 tap ./test/*.ts",
    "test:citree": "cd tools/citree && npm test",
    "snap": "npm run build:versions && npm run format && NO_TRACE=1 tap ./test/*.ts --snapshot",
    "console": "npm run build && ts-node",
    "prepare": "husky",
    "linecount": "bash ./scripts/linecount.sh",
    "clean": "rm -rf ./dist"
  },
  "keywords": [
    "basic",
    "shell",
    "language",
    "interpreter"
  ],
  "author": "Josh Holbrook",
  "license": "MIT",
  "dependencies": {
    "@jfhbrook/citree": "file:tools/citree",
    "@types/text-table": "^0.2.5",
    "dotenv": "^16.4.5",
    "strftime": "^0.10.2",
    "text-table": "^0.2.0",
    "typescript-parsec": "^0.3.4"
  },
  "devDependencies": {
    "@jfhbrook/swears": "^1.0.2",
    "@types/strftime": "^0.9.8",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.42.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.0",
    "exercise-bike": "^2.0.1",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier": "^3.0.0",
    "source-map-support": "^0.5.21",
    "tap": "^18.7.2",
    "ts-loader": "^9.4.3",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.1.3"
  }
}
