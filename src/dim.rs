// BASIC arrays. I'm not ready to implement yet, but I have grand designs so I
// want to start this file to take notes.

pub struct Array {
}

impl Array {
    // if specified, dimensions are used to initialize the size of the
    // underlying tensor. if unspecified, a dynamic array is used.
    fn new(dimensions: Vec<i64>) -> Array {
        Array { }
    }
}
